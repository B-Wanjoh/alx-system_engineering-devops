-The script inside the file "0-iam_betty"  switches the current user to the user betty.
-The script inside the file "1-who_am_i" prints the effective username of the current user.
-The script inside the file "2-groups" prints all the groups the current user is part of.
-The script inside the file "3-new_owner" changes the owner of the file hello to the user betty.
-The script inside the file "4-empty" creates an empty file called hello.
-The script inside the file "5-execute" adds execute permission to the owner of the file hello.
-The script inside the file "6-multiple_permissions" adds execute permission to the owner and the group owner, and read permission to other users, to the file hello.
-The script inside the file "7-everybody" adds execution permission to the owner, the group owner and the other users, to the file hello.
-The script inside the file "8-James_Bond" sets specified permission to the file hello.(007)
-The script inside the file "9-John_Doe" sets a specified mode to the file hello.(763)
-The script inside the file "10-mirror_permissions" sets the mode of the file hello the same as olleh’s mode.
-The script inside the file "11-directories_permissions" adds execute permission to all subdirectories of the current directory for the owner, the group owner and all other users.
-The script inside the file "12-directory_permissions" creates a directory called my_dir with permissions 751 in the working directory.
-The script inside the file "13-change_group" changes the group owner to school for the file hello.
